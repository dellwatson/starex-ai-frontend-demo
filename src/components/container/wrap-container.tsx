// this become accordion when on mobile, and shows the title here, and only accept col span
import React from "react";

export default function Default() {
  return (
    <div>
      Header
      {/* ai stuff, connect, amounts, hamburger */}
    </div>
  );
}
